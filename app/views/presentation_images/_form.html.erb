<%= form_for [@supplier, @presentation, @presentation_image],
		:url => supplier_account_presentation_presentation_images_path(@supplier),
    :validate => true,
		:html => {:multipart => true} do |f| %>
		
	<%= render 'form_content', :f => f %>

<% end %>
