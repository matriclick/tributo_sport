  <% if @tip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tip.errors.count, "error") %> prohibited this ceremony from being saved:</h2>

      <ul>
      <% @tip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
	<% end %>
	<br />
	<div class="field">
  	<%= f.label :question %><br />
  	<%= f.text_area :question, :size => "20x4" %>
	</div>
	<br />
	<div class="field">
  	<%= f.label :answer %><br />
  	<%= f.text_area :answer, :size => "20x20" %>
	</div>
	<br />
  <div class="actions">
    <%= f.submit %>
  </div>
<div class="clear"></div>
