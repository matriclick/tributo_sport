<%= form_for(@challenge_activity) do |f| %>
  <% if @challenge_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@challenge_activity.errors.count, "error") %> prohibited this challenge_activity from being saved:</h2>

      <ul>
      <% @challenge_activity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :matriclicker_id, :value => @matriclicker.id %>
  </div>
<br />
  <div class="field">
    <%= f.label :challenge_activity_type_id, 'Tipo de actividad' %>	<%= f.collection_select :challenge_activity_type_id, ChallengeActivityType.order('name ASC'), :id, :name, :prompt => "Select", :allow_blank => false %>
  </div>
<br />
  <div class="field">
    <%= f.label :comments %>
    <%= f.text_area :comments %>
  </div>
<br />
    <%= f.hidden_field :challenge_id, :value => @challenge.id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
