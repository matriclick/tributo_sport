<div style="float:right">
    <%= link_to 'Volver', administration_index_path, :class => :button %>
</div>
<div class="titulo_style" style="float:left">
	<h1>Rubros</h1>
</div>

<table>
  <tr>
    <th>Nombre</th>
    <th>Posición</th>
  </tr>

<% @industry_categories.each do |industry_category| %>
  <tr>
    <td><%= industry_category.get_name %></td>
	<td>
		<%= form_for(industry_category) do |f| %>
			<%= f.number_field :position, :size => 5 %>
			<%= f.submit %>
		<% end %>
	</td>
  </tr>
<% end %>
</table>

<br />
